---
const {
  title,
  videoFileName = "home.mp4",
  logo = "/images/logos/logo-limmat.svg",
} = Astro.props;
const videoPath = `/videos/${videoFileName}`; // Esta es la ruta correcta
---

<section
  class="relative w-full h-screen lg:h-[80vh] xl:h-[70vh] 2xl:h-[60vh] overflow-hidden"
>
  <video
    autoplay
    muted
    loop
    playsinline
    class="absolute inset-0 object-cover w-full h-full"
  >
    <source src={videoPath} type="video/mp4" />
    Tu navegador no soporta la etiqueta de video.
  </video>

  <div class="absolute inset-0 bg-black opacity-25"></div>

  <div class="relative z-10 flex items-center justify-center h-full">
    <div class="text-gray-300">
      <img
        src={logo}
        alt="Limmat Logo"
        class="mx-auto mb-4"
        width="400px"
        height="115px"
      />
      <h1
        class="text-3xl md:text-4xl font-bold text-gray-300 uppercase text-center"
      >
        {title}
      </h1>
    </div>
  </div>
</section>
