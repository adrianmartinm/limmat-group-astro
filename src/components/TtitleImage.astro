---
const { title = "", imageUrl = "" } = Astro.props;
---

<div class="flex flex-col items-center p-6">
  <h3 class="text-3xl font-semibold text-limmat text-center mb-4">
    {title}
  </h3>
  <div class="w-full h-96 overflow-hidden rounded-md">
    <img src={imageUrl} alt={title} class="w-full h-full object-fit" />
  </div>
</div>
